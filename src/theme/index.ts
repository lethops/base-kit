import * as baseTheme from './theme';
import * as buttonTheme from '../components/Button/styles/theme';
import * as buttonColorTheme from '../components/Button/styles/theme/color';

export default {
  ...baseTheme,
  ...buttonTheme,
  ...buttonColorTheme,
};
